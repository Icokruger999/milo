{
    "Parameters": {
        "commands": [
            "echo 'Checking backend logs...'",
            "tail -50 /home/ec2-user/backend.log | grep -i 'incident\\|error\\|exception' || echo 'No errors found'",
            "echo ''",
            "echo 'Checking if backend is running...'",
            "ps aux | grep 'Milo.API.dll' | grep -v grep"
        ]
    }
}
