{
    "Parameters": {
        "commands": [
            "cd /home/ec2-user/milo",
            "chmod +x start-backend-service.sh",
            "./start-backend-service.sh",
            "sleep 5",
            "echo 'Checking if backend is running...'",
            "ps aux | grep 'Milo.API.dll' | grep -v grep || echo 'Backend not running yet'",
            "echo 'Checking port 5001...'",
            "netstat -tlnp | grep 5001 || echo 'Port 5001 not listening yet'"
        ]
    }
}
