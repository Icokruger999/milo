{
  "InstanceIds": ["i-06bc5b2218c041802"],
  "DocumentName": "AWS-RunShellScript",
  "Parameters": {
    "commands": [
      "echo 'This fix requires rebuilding the DLL - cannot patch binary directly'",
      "echo 'Current DLL info:'",
      "ls -lh /home/ec2-user/milo-backend-publish/Milo.API.dll",
      "echo ''",
      "echo 'The issue: System.Net.Mail IsBodyHtml is not being set correctly'",
      "echo 'Solution: Need to rebuild DLL with MailKit library instead'",
      "echo ''",
      "echo 'For now, emails will continue to show HTML as text until DLL is rebuilt'"
    ]
  }
}
