{
    "Parameters": {
        "commands": [
            "cd /home/ec2-user/milo",
            "chmod +x setup-cron-daily-reports.sh",
            "./setup-cron-daily-reports.sh",
            "echo 'Verifying cron job...'",
            "crontab -l | grep 'send-daily-reports' || echo 'Cron job not found'"
        ]
    }
}
