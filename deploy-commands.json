{
  "commands": [
    "sudo systemctl stop milo-backend.service",
    "cd /home/ec2-user",
    "if [ -d milo-backend-publish ]; then cd milo-backend-publish && git pull origin main || (cd .. && rm -rf milo-backend-publish && git clone https://github.com/Icokruger999/milo.git temp-repo && mkdir -p milo-backend-publish && cp -r temp-repo/backend/Milo.API/publish/* milo-backend-publish/ && rm -rf temp-repo); else git clone https://github.com/Icokruger999/milo.git temp-repo && mkdir -p milo-backend-publish && cp -r temp-repo/backend/Milo.API/publish/* milo-backend-publish/ && rm -rf temp-repo; fi",
    "sudo systemctl start milo-backend.service",
    "sleep 3",
    "sudo systemctl status milo-backend.service --no-pager -l"
  ]
}
